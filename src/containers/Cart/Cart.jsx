import PropTypes from 'prop-types';
import styles from "./Cart.module.css";

const Cart = () => {
    return (
        <>
            Cart
        </>
    );
}

Cart.propTypes = {
     propName: PropTypes.string
}

export default Cart;
